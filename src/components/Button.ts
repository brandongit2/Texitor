import styled from "styled-components";

export const Button = styled.button`
    background: var(--color-1);
    color: var(--color-5);
    border-radius: 8px;
    padding: 0.5rem 1rem;
    cursor: pointer;
`;
